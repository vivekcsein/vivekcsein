{
  "name": "vivekcsein",
  "description": "Personal Github profile and projects",
  "module": "server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch ./src/server.ts",
    "start": "bun run server.ts",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "prettier": "^3.8.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
